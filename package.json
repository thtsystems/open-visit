{
  "name": "open-visit",
  "version": "1.0.0",
  "description": "Open-source application for visiting scheduling.",
  "author": "",
  "license": "MIT",
  "scripts": {
    "wrangler": "wrangler",
    "dev": "turbo dev --no-cache --continue --parallel --concurrency=1000",
    "db:push": "turbo db:push",
    "db:generate": "turbo db:generate",
    "db:studio": "turbo db:studio"
  },
  "dependencies": {
    "prettier": "^3.0.3",
    "turbo": "^1.10.16",
    "typescript": "^5.2.2",
    "wrangler": "^3.14.0"
  }
}
